{
  "name": "neurascore",
  "version": "1.0.0",
  "description": "Data analytics platform for measuring organizational data literacy and maturity",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd server && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd server && npm start",
    "install:all": "npm install && cd frontend && npm install && cd ../server && npm install && cd ../python-services && pip install -r requirements.txt",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "data-analytics",
    "data-literacy",
    "dashboard",
    "visualization",
    "nlp"
  ],
  "author": "NeuraScore Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.12.2"
  }
}
